on:
  workflow_dispatch:
    inputs:
      environment:
        description: "enter the environment to deploy to"
        required: true
      version_identity:
        description: "Identity: enter image tag"
        required: false
